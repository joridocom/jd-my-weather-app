FR01: The system must display current weather information (temperature, relative humidity, and current status) for Tokyo and Nasushiobara. -> No test linked (Not Tested)
FR02: The system should fetch this data from the JAXA Earth API at regular intervals (at least every 60 minutes) or on-demand when the user loads or refreshes the page. -> No test linked (Not Tested)
FR03: Provide a simple, clean webpage accessible on modern desktop and mobile browsers. -> No test linked (Not Tested)
FR04: Show at a minimum the current temperature, relative humidity, time, and the icon for the current weather. -> No test linked (Not Tested)
FR05: Include a clear label or timestamp indicating when the weather information was last updated. -> No test linked (Not Tested)
FR06: Display both Tokyo and Nasushiobara on the same screen. -> Location names are displayed correctly (Passed)
FR07: If the JAXA Earth API is unavailable or returns errors, display a friendly notification to the user (e.g., “Weather data currently unavailable”). -> No test linked (Not Tested)
FR08: Log errors for troubleshooting. -> No test linked (Not Tested)
FR09: The app should cache weather data for a short period (the past 2 hours) to reduce excessive API calls and page load times. -> No test linked (Not Tested)
FR10: Users can manually refresh if needed. -> No test linked (Not Tested)
NFR01: The webpage should load within 2–3 seconds on average broadband connections. -> No test linked (Not Tested)
NFR02: API calls should be optimized to avoid delays. -> No test linked (Not Tested)
NFR03: Minimal resource usage to remain cost-efficient. -> No test linked (Not Tested)
NFR04: The app should be available 24/7 with minimal downtime. -> No test linked (Not Tested)
NFR05: Use robust hosting and monitor the availability of both the app and the JAXA Earth API. -> No test linked (Not Tested)
NFR06: It should handle moderate traffic spikes (e.g. if many users check the weather at the same time). -> No test linked (Not Tested)
NFR07: Cloud-based or container-based infrastructure can be used to scale if user load increases. -> No test linked (Not Tested)
NFR08: Secure all communications (HTTPS). -> No test linked (Not Tested)
NFR09: Handle API keys securely (do not expose them in client-side code). -> No test linked (Not Tested)
NFR10: Code should be structured to allow easy updates if the JAXA Earth API changes or if additional features (e.g., more locations) are added. -> No test linked (Not Tested)
NFR11: Documentation for setup and deployment is provided so new developers can onboard quickly. -> No test linked (Not Tested)
NFR12: Basic logs for API responses and errors. -> No test linked (Not Tested)
NFR13: Application monitoring or analytics to track usage and detect issues (e.g., performance logs, error rates). -> No test linked (Not Tested)
NFR14: Comply with JAXA Earth API usage policies (e.g., not exceeding daily rate limits). -> No test linked (Not Tested)
NFR15: Implement graceful handling or fallback if rate limits are reached. -> No test linked (Not Tested)

Total: 25, Tested: 1 (4.00%), Passed: 1 (4.00%)
